dev() {
  if [[ "$1" == "-ls" ]]
  then
    tree -L 2 ~/code
  else
    cd ~/code/$1
  fi
}

dotfiles() {
  if [ -d ~/.dotfiles/$1/.config ]
  then
    cd ~/.dotfiles/$1/.config/$1
  else
    cd ~/.dotfiles/$1
  fi
}
