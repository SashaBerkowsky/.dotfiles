# ~/.config/wofi/config

# --- General Settings ---
# Which mode to show when Wofi starts. Common modes: drun (desktop entries), run (executables), dmenu (stdin/stdout)
mode=drun

# Width of the Wofi window (in pixels or percent, e.g., 50%)
width=50%
# Height of the Wofi window (in pixels or percent)
height=400px # Or 40%

# Position on the screen (center, top, bottom, left, right, top-left, etc.)
# xoffset and yoffset can be used for fine-tuning
location=center

# Number of lines to display at once (overrides height if set)
# lines=10

# Number of columns (useful for grid layouts)
columns=1

# Whether to hide the scrollbar
hide_scroll=true

# Matching mode: contains, multi-contains, fuzzy
matching=fuzzy

# Prompt text displayed in the search box
# prompt=Run:

# Use dark GTK theme variant
# gtk_dark=true

# Allow rendering images (icons for drun entries)
allow_images=false

# Layer to open on (background, bottom, top, overlay)
layer=overlay # 'overlay' is often good for launchers

# --- Keybindings (some can be set here, others in Hyprland) ---
# See 'man wofi-keys' for key names
# key_up=Up
# key_down=Down
# key_submit=Return
# key_exit=Escape

# --- Mode-specific options (prefix with mode name, e.g., dmenu-parse_action) ---
# dmenu-parse_action=true # For dmenu mode, if you want to parse actions

# --- Specify custom CSS file (if not in default location) ---
# style=/path/to/your/custom/wofi.css
